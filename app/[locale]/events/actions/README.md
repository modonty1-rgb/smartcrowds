# Events Actions

This folder contains server actions specific to the Events pages.

## Files
- `actions.ts` - Server actions for listing and retrieving events for public pages

## Functions
- `listEvents()` - List all events (with optional filtering for published events)
- `getEventById(id)` - Get a single event by ID (with optional published check)

